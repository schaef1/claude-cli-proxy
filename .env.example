# claude-cli-proxy environment variables
# Copy this to .env and fill in real values (never commit .env)

# Port the proxy listens on (default: 19000)
# PORT=19000

# Path to the Claude CLI binary
# CLAUDE_CLI=/home/rob/.local/bin/claude
